# Audio assets

## Answer feedback (shipped)

- `correct.wav` — “You're correct”
- `wrong.wav` — “Good try”

Played by the quiz Check Answer flow via the HTML Audio API (preloaded static clips).

## Word pronunciation (future)

Place native-speaker pronunciation audio here (mp3/ogg), keyed by word id.
Until then, the Volume button uses browser speech synthesis.
